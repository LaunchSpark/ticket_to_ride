"""Shared ML helpers for external bots.

Modules in this package must stay importable by the bot loader on a base
install. Optional ML dependencies belong behind lazy imports inside runtime
functions.
"""
